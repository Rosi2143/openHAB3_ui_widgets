uid: main_widget_Security_Card
tags: []
props:
  parameters:
    - context: item
      description: Security Mode Item
      label: Security Mode
      name: securityMode
      required: true
      type: TEXT
    - context: item
      description: Security PIN Item
      label: Security PIN
      name: referencePIN
      required: false
      type: TEXT
timestamp: Nov 27, 2022, 10:31:17 PM
component: f7-card
config:
  style:
    --f7-card-header-border-color: transparent
    background: =(themeOptions.dark=="light") ? ("white"):("black")
    border-radius: 10px
    margin-left: 5px
    margin-right: 5px
slots:
  default:
    - component: f7-card-content
      config:
        style:
          height: auto
      slots:
        default:
          - component: f7-row
            config:
              style:
                justify-content: space-between
            slots:
              default:
                - component: f7-col
                  config:
                    style:
                      text-align: center
                  slots:
                    default:
                      - component: oh-button
                        config:
                          action: popup
                          actionModal: widget:main_widget_Security_Keypad
                          actionModalConfig:
                            referencePIN: =props.referencePIN
                            securityMode: =props.securityMode
                            targetMode: armed-home
                          class:
                            - padding-top-half
                            - display-flex
                            - flex-direction-column
                          fill: '=vars.objVar.selectThing=="Security" ? true : false'
                          icon-f7: checkmark_shield_fill
                          iconColor: '=items[props.securityMode].state != "armed-home"  ? "gray" : "red"'
                          iconSize: 60
                          style:
                            --f7-button-bg-color: transparent
                            --f7-button-hover-bg-color: '=vars.objVar.selectThing=="Security" ? "F8BB00" : "transparent"'
                            --f7-button-padding-horizontal: 0px
                            --f7-button-padding-vertical: 0px
                            --f7-button-text-color: 'themeOptions.dark=="light" ? white : black'
                            font-size: 14px
                            height: auto
                          text: ARMED HOME
                - component: f7-col
                  config:
                    style:
                      text-align: center
                  slots:
                    default:
                      - component: oh-button
                        config:
                          action: popup
                          actionModal: widget:main_widget_Security_Keypad
                          actionModalConfig:
                            referencePIN: =props.referencePIN
                            securityMode: =props.securityMode
                            targetMode: disarmed
                          class:
                            - padding-top-half
                            - display-flex
                            - flex-direction-column
                          fill: '=vars.objVar.selectThing=="Security" ? true : false'
                          icon-f7: checkmark_shield_fill
                          iconColor: '=items[props.securityMode].state != "disarmed"  ? "gray" : "green"'
                          iconSize: 60
                          style:
                            --f7-button-bg-color: transparent
                            --f7-button-hover-bg-color: '=vars.objVar.selectThing=="Security" ? "F8BB00" : "transparent"'
                            --f7-button-padding-horizontal: 0px
                            --f7-button-padding-vertical: 0px
                            --f7-button-text-color: 'themeOptions.dark=="light" ? white : black'
                            font-size: 14px
                            height: auto
                          text: DISARMED
                - component: f7-col
                  config:
                    style:
                      text-align: center
                  slots:
                    default:
                      - component: oh-button
                        config:
                          action: popup
                          actionModal: widget:main_widget_Security_Keypad
                          actionModalConfig:
                            referencePIN: =props.referencePIN
                            securityMode: =props.securityMode
                            targetMode: armed-away
                          class:
                            - padding-top-half
                            - display-flex
                            - flex-direction-column
                          fill: '=vars.objVar.selectThing=="Security" ? true : false'
                          icon-f7: checkmark_shield_fill
                          iconColor: '=items[props.securityMode].state != "armed-away"  ? "gray" : "red"'
                          iconSize: 60
                          style:
                            --f7-button-bg-color: transparent
                            --f7-button-hover-bg-color: '=vars.objVar.selectThing=="Security" ? "F8BB00" : "transparent"'
                            --f7-button-padding-horizontal: 0px
                            --f7-button-padding-vertical: 0px
                            --f7-button-text-color: 'themeOptions.dark=="light" ? white : black'
                            font-size: 14px
                            height: auto
                          text: ARMED AWAY
          - component: f7-list
            config:
              accordionList: true
              noHairlinesBetween: true
            slots:
              default:
                - component: f7-list-item
                  config:
                    accordionItem: true
                    style:
                      font-weight: bold
                      fontSize: 15px
                      margin-top: 15px
                    title: Door Sensors
                  slots:
                    default:
                      - component: f7-accordion-content
                        config:
                          accordionList: true
                          noHairlinesBetween: true
                        slots:
                          default:
                            - component: oh-repeater
                              config:
                                accordionList: true
                                fetchMetadata: semantics,widgetOrder,uiSemantics
                                for: securityGroupMember
                                fragment: true
                                itemTags: Door
                                sourceType: itemsWithTags
                              slots:
                                default:
                                  - component: oh-list
                                    config:
                                      noHairlinesBetween: true
                                    slots:
                                      default:
                                        - component: oh-repeater
                                          config:
                                            fetchMetadata: semantics,uiSemantics
                                            filter: (loop.doorMember.tags).includes("OpenState")
                                            for: doorMember
                                            fragment: true
                                            groupItem: =loop.securityGroupMember.name
                                            sourceType: itemsInGroup
                                          slots:
                                            default:
                                              - component: f7-list-item
                                                config:
                                                  style:
                                                    font-weight: 800
                                                    fontSize: 14px
                                                    margin-top: -7px
                                                  title: "=loop.doorMember.metadata.uiSemantics ? loop.doorMember.metadata.uiSemantics.config.equipment + loop.doorMember.metadata.uiSemantics.config.preposition + loop.doorMember.metadata.uiSemantics.config.location : loop.doorMember.name"
                                                slots:
                                                  default:
                                                    - component: f7-chip
                                                      config:
                                                        bgColor: transparent
                                                        iconColor: '=loop.doorMember.state != "CLOSED" ? "red" : "gray"'
                                                        iconMaterial: sensor_door
                                                        iconSize: 20px
                    inner-end:
                      - component: f7-chip
                        config:
                          bgColor: transparent
                          iconColor: '=loop.doorMember.state != "CLOSED" ? "red" : "gray"'
                          iconMaterial: sensor_door
                          iconSize: 20px
          - component: f7-list
            config:
              accordionList: true
              noHairlinesBetween: true
            slots:
              default:
                - component: f7-list-item
                  config:
                    accordionItem: true
                    style:
                      font-weight: bold
                      fontSize: 15px
                      margin-top: 15px
                    title: Window Sensors
                  slots:
                    default:
                      - component: f7-accordion-content
                        config:
                          accordionList: true
                          noHairlinesBetween: true
                        slots:
                          default:
                            - component: oh-repeater
                              config:
                                accordionList: true
                                fetchMetadata: semantics,widgetOrder,uiSemantics
                                for: securityGroupMember
                                fragment: true
                                itemTags: Window
                                sourceType: itemsWithTags
                              slots:
                                default:
                                  - component: oh-list
                                    config:
                                      noHairlinesBetween: true
                                    slots:
                                      default:
                                        - component: oh-repeater
                                          config:
                                            fetchMetadata: semantics,uiSemantics
                                            filter: (loop.windowMember.tags).includes("OpenState")
                                            for: windowMember
                                            fragment: true
                                            groupItem: =loop.securityGroupMember.name
                                            sourceType: itemsInGroup
                                          slots:
                                            default:
                                              - component: f7-list-item
                                                config:
                                                  style:
                                                    font-weight: 800
                                                    fontSize: 14px
                                                    margin-top: -7px
                                                  title: "=loop.windowMember.metadata.uiSemantics ? loop.windowMember.metadata.uiSemantics.config.equipment + loop.windowMember.metadata.uiSemantics.config.preposition + loop.windowMember.metadata.uiSemantics.config.location : loop.windowMember.name + 'asda'"
                                                slots:
                                                  default:
                                                    - component: f7-chip
                                                      config:
                                                        bgColor: transparent
                                                        iconColor: '=loop.windowMember.state != "CLOSED" ? "red" : "gray"'
                                                        iconMaterial: sensor_window
                                                        iconSize: 20px
                    inner-end:
                      - component: f7-chip
                        config:
                          bgColor: transparent
                          iconColor: '=loop.cameraMember.state != "CLOSED" ? "red" : "gray"'
                          iconMaterial: sensor_window
                          iconSize: 20px
          - component: f7-list
            config:
              accordionList: true
              noHairlinesBetween: true
            slots:
              default:
                - component: f7-list-item
                  config:
                    accordionItem: true
                    style:
                      font-weight: bold
                      fontSize: 15px
                      margin-top: 15px
                    title: Motion Sensors
                  slots:
                    default:
                      - component: f7-accordion-content
                        config:
                          accordionList: true
                          noHairlinesBetween: true
                        slots:
                          default:
                            - component: oh-repeater
                              config:
                                accordionList: true
                                fetchMetadata: semantics,widgetOrder,uiSemantics
                                for: securityGroupMember
                                fragment: true
                                itemTags: MotionDetector
                                sourceType: itemsWithTags
                              slots:
                                default:
                                  - component: oh-list
                                    config:
                                      noHairlinesBetween: true
                                    slots:
                                      default:
                                        - component: oh-repeater
                                          config:
                                            fetchMetadata: semantics,uiSemantics
                                            filter: (loop.motionMember.tags).includes("Presence")
                                            for: motionMember
                                            fragment: true
                                            groupItem: =loop.securityGroupMember.name
                                            sourceType: itemsInGroup
                                          slots:
                                            default:
                                              - component: f7-list-item
                                                config:
                                                  style:
                                                    font-weight: 800
                                                    fontSize: 14px
                                                    margin-top: -7px
                                                  title: "=loop.motionMember.metadata.uiSemantics ? loop.motionMember.metadata.uiSemantics.config.equipment + loop.motionMember.metadata.uiSemantics.config.preposition + loop.motionMember.metadata.uiSemantics.config.location : loop.motionMember.name"
                                                slots:
                                                  default:
                                                    - component: f7-chip
                                                      config:
                                                        bgColor: transparent
                                                        iconColor: '=loop.motionMember.state != "OFF" ? "red" : "gray"'
                                                        iconMaterial: sensors
                                                        iconSize: 20px
                    inner-end:
                      - component: f7-chip
                        config:
                          bgColor: transparent
                          iconColor: '=loop.motionMember.state != "OFF" ? "red" : "gray"'
                          iconMaterial: sensors
                          iconSize: 20px
          - component: f7-list
            config:
              accordionList: true
              noHairlinesBetween: true
            slots:
              default:
                - component: f7-list-item
                  config:
                    accordionItem: true
                    style:
                      font-weight: bold
                      fontSize: 15px
                      margin-top: 15px
                    title: Surveillance
                  slots:
                    default:
                      - component: f7-accordion-content
                        config:
                          accordionList: true
                          noHairlinesBetween: true
                        slots:
                          default:
                            - component: oh-repeater
                              config:
                                accordionList: true
                                fetchMetadata: semantics,widgetOrder,uiSemantics
                                for: securityGroupMember
                                fragment: true
                                itemTags: Camera
                                sourceType: itemsWithTags
                              slots:
                                default:
                                  - component: oh-list
                                    config:
                                      noHairlinesBetween: true
                                    slots:
                                      default:
                                        - component: oh-repeater
                                          config:
                                            fetchMetadata: semantics
                                            for: cameraMember
                                            fragment: true
                                            groupItem: =loop.securityGroupMember.name
                                            sourceType: itemsInGroup
                                          slots:
                                            default:
                                              - component: f7-list-item
                                                config:
                                                  style:
                                                    font-weight: 800
                                                    fontSize: 14px
                                                    margin-top: -7px
                                                  title: =loop.cameraMember.label
                                                slots:
                                                  default:
                                                    - component: f7-chip
                                                      config:
                                                        bgColor: transparent
                                                        iconColor: '=loop.cameraMember.state != "OFF" ? "red" : "gray"'
                                                        iconMaterial: video_camera_front
                                                        iconSize: 20px
                    inner-end:
                      - component: f7-chip
                        config:
                          bgColor: transparent
                          iconColor: '=loop.cameraMember.state != "CLOSED" ? "red" : "gray"'
                          iconMaterial: video_camera_front
                          iconSize: 20px
